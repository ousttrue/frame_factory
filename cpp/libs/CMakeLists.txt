#
# json
#
SET(TARGET_NAME json)
SET(SRC_DIR ${CMAKE_CURRENT_LIST_DIR}/../_external/json)
ADD_LIBRARY(${TARGET_NAME} INTERFACE)
TARGET_INCLUDE_DIRECTORIES(${TARGET_NAME} INTERFACE
    ${SRC_DIR}/include
)

SUBDIRS(imgui SDL2)
